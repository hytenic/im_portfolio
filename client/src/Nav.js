import React from "react";

const Nav = () => (
    <nav className="navbar">
        <div className="menuContainer">
            <a className={"aboutMe " + "eachMenu"}>
                <span>About Me</span>
            </a>
        </div>
    </nav>
);

export default Nav;